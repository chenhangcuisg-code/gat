# Cases — one paragraph per finished game or milestone

What / paradigm / hard part / result / provenance. Append, dated.

---

## 2026-07-16 · (seed) "Ember and Ink" — worked example
GAT's bundled example: a papercut-wuxia idle-RPG. Ran `/gat-brainstorm` → `/gat-story` →
`/gat-design` → `/gat-milestone` to produce the design docs, then `/gat-style-lock` froze a
"flat papercut, Song-dynasty ink, single vermilion accent" contract. Demonstrates the
design→style-lock handoff; downstream implementation (`/gat-scaffold`→`/gat-implement`→
`/gat-verify`) is left as the reader's exercise. See `examples/ember-and-ink/`.

<!-- Append new cases above this line. -->
